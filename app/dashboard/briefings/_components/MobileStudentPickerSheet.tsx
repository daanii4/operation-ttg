/**
 * Sprint 5 location-of-record. Sprint 6 promoted the implementation to a
 * shared component so Trajectory and Eligibility can reuse it. This file
 * keeps the briefings import path stable.
 */
export {
  MobileStudentPickerSheet,
  default,
  type MobileStudentPickerSheetProps,
} from "@/components/dashboard/MobileStudentPickerSheet";
